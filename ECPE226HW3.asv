% Alison Shu, Jeffrey Dwyer
% Dr. Mueller
% ECPE 226
% 9/25/2017
% HW3 - Logistic Regression


File = load('usps_modified.mat');
raw_data = File.data;
[img, example, digit] = size(raw_data);
lr = 0.1; % learning rates of 0.1, 1, 10, 50.
N = example;

ones = clean(data(:,:,1), 1);

others = clean(data(:,n,[2:10]), -1);

training = [ones(1:400); others(1:400)];
testing = [ones(400:500); others(400:500)];







